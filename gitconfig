[user]
    name = Matt Perpick
    email = matthewperpick@gmail.com

[alias]
    st = status
    ci = commit
    br = branch
    co = checkout
    df = diff
    lg = log -p
    up = "!gup() { git fetch $1 && git rebase -p $1 $2; }; gup"
    stashes = stash list
    unstage = reset HEAD

[core]
    whitespace = trailing-space,space-before-tab,cr-at-eol

[color]
    ui = true
    diff = true
