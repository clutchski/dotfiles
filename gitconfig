[user]
    name = Matt Perpick
    email = matthewperpick@gmail.com

[alias]
    st = status
    ci = commit
    br = branch
    co = checkout
    df = diff
    lg = log -p
    up = "!gup() { git fetch origin && git rebase -p origin ${current_branch}; }; gup"
    stashes = stash list
    unstage = reset HEAD
    showstash = stash show -p

[core]
    whitespace = trailing-space,space-before-tab,cr-at-eol

[color]
    ui = true
    diff = true
