# tmux config for wolf.

project_name: wolf
project_root: ~/code/github/wolf
rvm: ruby-1.9.2-p290@timber
tabs:
  - built: rake build && vim build/
  - guard: rake build && guard
  - rakefile: vim rakefile
  - code: vim src
  - tests: vim tests
